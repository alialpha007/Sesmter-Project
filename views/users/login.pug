extends ../layout 

block content
    div.mt-5.ml-4.mb-4.mr-4.m-auto.w-50.card.p-5.bg-dark
        form(action="/login", method="post", class="text-white")
            h1.text-center Login
            div.form-group
            label(for="title" class="text-white") Email
            input(type="email" name="email" class="form-control mt-3" id="email" placeholder="Enter your Email" value="test")
            div.form-group.mt-3
            label(for="text" class="text-white") Password
            input(name="password" type="password" class="form-control" id="password" placeholder="Password" value="test")
            a(href="/register", class="text-white d-block pt-3") Didn’t have an account? Register
            button(type="submit" class="btn btn-primary pt-2 mt-3 mr-1") Login